import BrowseBuilds from "@/components/tools/BrowseBuilds/BrowseBuilds";

export const metadata = {
  title: "Browse Talent Builds | NoobShroom",
  description: "Browse community talent builds with ratings and filters",
};

export default function BrowseBuildsPage() {
  return <BrowseBuilds />;
}
