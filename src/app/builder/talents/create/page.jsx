import TalentBuilder from "@/components/builder/talents/TalentBuilder";

export const metadata = {
  title: "Create Talent Build | NoobShroom",
  description: "Create and customize your talent build",
};

export default function CreateTalentBuilderPage() {
  return <TalentBuilder />;
}
